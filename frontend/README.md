# Code Trace Studio frontend

The React/Vite interface expects the Flask backend at `http://localhost:5001`.

```bash
npm install
npm run dev
```

Use `npm test`, `npm run lint`, and `npm run build` before committing frontend changes.
